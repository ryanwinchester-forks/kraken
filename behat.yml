default:
    suites:
        default:
            paths:    [ %paths.base%/tests/features ]
            contexts: [ Tests\Contexts\FeatureContext ]
    extensions:
        Laracasts\Behat: ~
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~
